all:
  children:
    homelab:
      hosts:
        orangepi:
          ansible_host: 192.168.0.7
          ansible_user: imotfnir
          ansible_ssh_private_key_file: ~/.ssh/orangepi2
      vars:
        ansible_become_pass: "{{ vault_ansible_become_pass }}"

    cloud:
      hosts:
        vps:
          ansible_host: 35.212.159.24
          ansible_user: imotfnir
          ansible_ssh_private_key_file: ~/.ssh/google_vps
      vars:
        ansible_become: true

    test:
      hosts:
        ansible-test:
          ansible_connection: docker